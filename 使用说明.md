# WPS 表格按列拆分工具（统信 UOS 版）

按**指定列的内容**把一个 WPS 表格拆（分割）成多个 WPS 表格，新表格**自动以分类名称命名**，
并且**完整沿用原表的模板格式**。

> 例：一张 3000 行的工资总表，按「部门」列拆分 → 得到 `财务部.xlsx`、`销售部.xlsx`、`技术部.xlsx`……
> 每个表里只有本部门的数据，字体、边框、底色、行高、列宽、数字格式、冻结窗格、自动筛选跟原表一模一样。

---

## 一、特点

| 特点 | 说明 |
|---|---|
| **★ 保留原模板格式** | 采用「模板克隆」方式：直接复制原文件的样式表，只替换数据行。字体、字号、颜色、边框、底色、行高、列宽、数字格式（日期/金额/百分比）、对齐、冻结窗格、自动筛选、合并单元格、条件格式全部保留 |
| **零依赖** | 只用 Python 标准库，**不用装 pandas / openpyxl**，内网和离线环境直接可用 |
| **中文友好** | 文件名为中文分类名；CSV 清单用 UTF-8 BOM，WPS 打开不乱码 |
| **数据保真** | 保留公式（并自动改写行号引用）、日期、数字、布尔、百分比等单元格类型 |
| **文件名安全** | 自动清理 `\ / : * ? " < > \|` 等非法字符，超长自动截断，重名自动加 `_2` |
| **三种用法** | 图形界面 / 交互式向导 / 命令行（可写进脚本批量跑） |
| **安全** | 提供「预演」模式，先看分类和行数，确认无误再写文件 |

---

## 二、安装与启动

1. 把整个 `wps-split` 文件夹拷到 UOS 电脑上（U 盘、微信传输均可）。
2. 首次使用先给脚本加执行权限（只需一次）：
   ```bash
   cd wps-split
   chmod +x 运行.sh 安装桌面快捷方式.sh
   ```
3. 启动（三选一）：
   - **图形界面**：双击 `运行.sh` → 选「运行」/「在终端中运行」
   - **桌面快捷方式**：先执行 `./安装桌面快捷方式.sh`，以后双击桌面图标即可
   - **命令行**：`python3 wps_split.py 文件名.xlsx -c 列名`

> 若双击 `运行.sh` 被文本编辑器打开：在文件管理器右键该文件 →「属性」→ 勾选「允许作为程序执行」，或右键 →「运行」。

统信 UOS 自带 Python3，一般无需安装任何东西。图形界面需要 tkinter，如弹窗提示缺失：
```bash
sudo apt install python3-tk     # 有外网/内网源时
```
没有 tkinter 也没关系，脚本会自动退回**命令行向导模式**。

---

## 三、图形界面用法

1. 点「浏览…」选择要拆分的表格；
2. 选工作表、确认表头行号（**表头在第几行就填几**，默认第 1 行），点「读取列」；
3. 在「拆分依据列」选择或输入列名（也支持列字母 `C` 或序号 `3`）；
4. 输出目录留空则自动在源文件旁建 `xxx_拆分结果`；
5. 勾选需要的选项（**「★ 沿用原表模板格式」默认已勾选**）→ 点「开始拆分」。

想先看看效果就点「**预演（只看统计）**」，不产生任何文件。

---

## 四、命令行用法

```bash
# 最常用：按“部门”列拆分
python3 wps_split.py 工资表.xlsx -c 部门

# 表头不在第一行时（例如第 1 行是大标题、第 2 行才是表头）
python3 wps_split.py 工资表.xlsx -c 部门 --header-row 2

# 用列字母（C 列）或列序号（第 3 列）指定
python3 wps_split.py 工资表.xlsx -c C
python3 wps_split.py 工资表.xlsx -c 3

# 指定输出目录
python3 wps_split.py 工资表.xlsx -c 部门 -o /home/uos/桌面/拆分结果

# 按两列组合拆分（部门+年份），文件名形如“财务部-2026.xlsx”
python3 wps_split.py 工资表.xlsx -c 部门,年份 --sep "-"

# 指定工作表（名称或序号）
python3 wps_split.py 工资表.xlsx -c 部门 --sheet 第二页

# 一个工作簿里的每个工作表都拆（按工作表名分文件夹存放）
python3 wps_split.py 工资表.xlsx -c C --all-sheets

# 批量拆分整个目录下的所有表格
python3 wps_split.py --dir ./待拆分 -c 部门

# 只看表格有哪些工作表、表头是什么（不拆分）
python3 wps_split.py 工资表.xlsx --info

# 预演：只统计分类和行数，不写文件
python3 wps_split.py 工资表.xlsx -c 部门 --dry-run

# 不带参数运行 = 交互式向导，一路回车即可
python3 wps_split.py
```

### 全部参数

| 参数 | 作用 |
|---|---|
| `-c, --column` | **必填**（批量模式也必填）。拆分依据列：列名 / 列字母 / 列序号，多列用英文逗号分隔 |
| `-o, --output-dir` | 输出目录，默认在源文件旁建 `xxx_拆分结果` |
| `--sheet` | 工作表名或序号（从 0 开始），默认 0 |
| `--all-sheets` | 对所有工作表都执行拆分 |
| `--header-row N` | 表头所在行，默认 1；填 0 表示没有表头 |
| `--no-header` | 拆分后的表不保留表头行（模板行收缩到表头之前） |
| `--template-rows N` | 每个分表**原样保留前 N 行**（多行标题时用），默认等于表头行号 |
| `--plain` | 普通模式：不沿用原模板格式（一般不用，仅作兜底） |
| `--skip-empty` | 分类为空的行直接丢弃，不生成「未分类」表 |
| `--empty-name` | 空值分类的命名，默认 `未分类` |
| `--no-trim` | 不去分类值首尾空格（默认会去掉，" 财务部 " 与 "财务部" 算同一类）|
| `--ignore-case` | 忽略英文大小写（`abc` 与 `ABC` 合并为一类）|
| `--sep` | 多列组合时分类名的连接符，默认 `-` |
| `--max-name-len` | 文件名最大字符数，默认 60 |
| `--sort` | 按分类名排序输出（默认按在表中出现的顺序）|
| `--no-summary` | 不生成 `拆分清单.csv` |
| `--dry-run` | 预演，只统计不写文件 |
| `--dir` | 批量处理指定目录下的所有表格 |
| `--info` | 只显示表格信息 |
| `--gui` | 启动图形界面 |
| `-q` | 静默模式 |

---

## 五、输出结果

拆分后在输出目录得到：

```
工资表_拆分结果/
├── 财务部.xlsx
├── 销售部.xlsx
├── 技术部_研发中心.xlsx     ← 原分类含"/"，自动替换成"_"
├── 未分类.xlsx              ← 该列为空的行
└── 拆分清单.csv             ← 分类名、行数、文件名的对照清单
```

### 每个分表的结构

```
┌─────────────────────────────────┐
│ 某某公司 2026 年员工工资台账         │ ← 模板行：原样保留，格式不变
│ 序号 │ 姓名 │ 部门 │ 工资 │ 日期 │ │ ← 表头：原样式 + 冻结
├──────┼──────┼──────┼──────┼─────┤
│  1   │ 赵某 │ 财务部│ 8,000│2020-01-05│ ← 只有本分类的数据行
│  8   │ 钱某 │ 财务部│ 9,200│2019-07-14│    边框/底色/数字格式全部沿用
└─────────────────────────────────┘
```

**保留的模板要素**：字体/字号/颜色、单元格底色、边框、行高、列宽、对齐方式、
数字格式（`# ,##0`、`yyyy-mm-dd`、`0.00`、`0%` 等）、冻结窗格、自动筛选（范围自动重设）、
合并单元格、条件格式、数据验证、超链接、公式（引用行号自动改写）。
输出文件只含目标工作表，不会带上原工作簿的其它页。

---

## 六、常见问题

**Q1：表头不在第 1 行（上面还有标题/制表日期）怎么办？**
加 `--header-row 2`（表头在第 2 行）。第 1 行会作为模板行原样保留在每个分表里。
如果标题占了多行，用 `--template-rows 3` 指定"前 3 行都是模板"。
注意：模板行里若含数据行，那几行会出现在**每一个**分表中，程序会给出提示。

**Q2：提示「是老式二进制表格（.xls/.et），本工具无法直接读取」？**
本工具读的是标准 `.xlsx` 格式。请用 WPS 打开那个文件 →「文件」→「另存为」→ 文件类型选
**Excel 工作簿(\*.xlsx)** → 保存后再拆分。

**Q3：中文乱码？**
CSV 清单已用 UTF-8 BOM 写入，WPS 直接双击打开正常。若用其他工具打开出现乱码，选择「UTF-8」编码即可。

**Q4：为什么出现 `abc.xlsx` 和 `ABC_2.xlsx` 两个文件？**
默认大小写算不同分类。为避免在大小写不敏感的文件系统上互相覆盖，第二个自动加了 `_2`。
若想合并成一类的，加 `--ignore-case`。

**Q5：分类太多（几百个）会不会很慢？**
300 行 × 9 个分类约 1 秒；10 万行级别通常在几十秒内完成。可先用 `--dry-run` 看分类数量。

**Q6：想把某一列强制按文本处理（如身份证号、长编号）？**
源表里这些单元格如果本来就是文本格式，拆分后仍是文本。若是数字格式且超过 15 位，Excel/WPS
本身就会丢精度，建议在源表中先把该列设为「文本」格式再保存。

**Q7：图片、图表、批注会保留吗？**
位于模板行区域的图片会随原样保留的部件一起带过来（表头 LOGO 之类）；
锚定在数据行上的浮动图片、图表不会跟着走。批注、数据透视表不保留。

**Q8：公式会不会算错？**
公式会保留，其中的行号引用会自动改写到新位置（如原第 12 行变成新第 5 行，`=D12*E12` 会变成 `=D5*E5`）。
跨表引用、或引用了被分到其它分表里的行的公式无法自动修正，WPS 打开时会重新计算，建议抽查。

---

## 七、文件清单

| 文件 | 说明 |
|---|---|
| `wps_split.py` | 拆分主程序：命令行 + 交互式向导 |
| `xlsx_template.py` | **模板克隆引擎**：复制原文件所有格式部件，只重写数据行 |
| `xlsx_lite.py` | 零依赖 xlsx 读写模块（读取 + 普通模式输出） |
| `wps_split_gui.py` | 图形界面（tkinter） |
| `运行.sh` | UOS 一键启动脚本 |
| `安装桌面快捷方式.sh` | 创建桌面图标 |
| `tests/make_data.py` | 生成测试数据（需 openpyxl，仅开发用） |
| `tests/make_template.py` | 生成带完整格式的模板样表（需 openpyxl） |
| `tests/verify.py` | 校验拆分结果与数据一致性（需 openpyxl） |
| `tests/verify_template.py` | 校验模板格式是否完整保留（需 openpyxl） |
| `tests/测试数据/` | 示例数据与拆分结果，可用来试手 |

---

## 八、自检（可选）

在有 openpyxl 的机器上，可跑一遍完整回归：

```bash
python3 tests/make_data.py                                             # 造数据
python3 tests/make_template.py                                         # 造带格式的模板样表
python3 wps_split.py tests/测试数据/员工工资表.xlsx -c 部门               # 拆分
python3 wps_split.py tests/测试数据/模板样表.xlsx -c 部门 --header-row 2  # 拆分（表头在第2行）
python3 tests/verify.py                                                # → 全部校验通过
python3 tests/verify_template.py                                       # → 模板格式全部保留
```
